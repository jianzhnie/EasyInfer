# ==============================================================================
# GLM-5.2 W8A8 PD + PP=2 分离部署
# ==============================================================================
# PNode: 2 组 PP=2 (每组 2 节点, TP=8 PP=2)
# DNode: 2 组 PP=2 (每组 2 节点, TP=4 PP=2)
# ==============================================================================

SSH_USER=root
SSH_PORT=22
SSH_CONNECT_TIMEOUT=10
CONTAINER_NAME=vllm-ascend-env
REMOTE_SCRIPT_DIR=/home/jianzhnie/llmtuner/llm/EasyInfer/examples/glm-5.2_w8a8/pd-separation

MODEL_PATH="/home/jianzhnie/llmtuner/hfhub/models/Eco-Tech/GLM-5.2-w8a8"
NIC_NAME="enp66s0f0"
LOG_DIR="/home/jianzhnie/llmtuner/llm/EasyInfer/logs"

# ---- PP=2 通用 ----
PP=2

# ---- Prefill: 2组 PP=2, 每组 TP=8 ----
# 组0: 194(head) + 195(worker), 组1: 196(head) + 197(worker)
P_TP_SIZE=8
P_DP_SIZE=2  # 2 PP groups
P_DP_SIZE_LOCAL=1
P_VLLM_START_PORT=9081
P_DP_RPC_PORT=16591

# ---- Decode: 2组 PP=2, 每组 TP=4 ----
# 组0: 198(head) + 199(worker), 组1: 200(head) + 201(worker)
D_TP_SIZE=4
D_DP_SIZE=2  # 2 PP groups
D_DP_SIZE_LOCAL=1
D_VLLM_START_PORT=9900
D_DP_RPC_PORT=16600

# ---- Ray 端口 ----
RAY_PORT=6379

# ---- PP Prefill 组 (head, worker) ----
P_PP_GROUPS=(
    "10.42.11.194 10.42.11.195"
    "10.42.11.196 10.42.11.197"
)
P_DP_ADDRESS="10.42.11.194"

# ---- PP Decode 组 (head, worker) ----
D_PP_GROUPS=(
    "10.42.11.198 10.42.11.199"
    "10.42.11.200 10.42.11.201"
)
D_DP_ADDRESS="10.42.11.198"
