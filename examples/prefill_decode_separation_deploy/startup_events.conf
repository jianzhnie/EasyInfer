# ==============================================================================
# startup_events.conf — 节点启动关注事件配置
# ==============================================================================
# 每行一个正则表达式，匹配节点日志中你关注的行。
# 匹配到的行会按日志顺序显示在界面，不重复显示已出现过的行。
#
# 正则默认大小写不敏感，用 .* 匹配任意可变内容（数字、路径等）。
# ==============================================================================

Starting to load model *
Loading weights took .* seconds
Loading model weights took .* GB
Started server process \[.*\]
Application startup complete\.
Uvicorn running on .*
The model is loaded
Loading checkpoint shard
Loaded .* weights
# WARNING              # 注释掉，只显示 ERROR/Traceback 级别
ERROR
Traceback
Exception
OOM
CUDA out of memory
Connection refused
Address already in use
